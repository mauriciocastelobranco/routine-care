<div class="chat-page">
  <div class="chat-shell">

    <div class="chat-top">
      <%= link_to "← #{@chat.patient.name}", patient_path(@chat.patient), class: "chat-back" %>

      <div class="chat-heading">
        <div class="chat-badge" aria-hidden="true"><span>💬</span></div>
        <h1 id="chat_title" class="chat-title"><%= @chat.title %></h1>
        <p class="chat-subtitle">Conversa relacionada ao paciente</p>
      </div>
    </div>

    <div class="chat-card">
      <div id="messages" class="chat-messages">
        <%= render "messages/messages", messages: @chat.messages %>
      </div>

      <div id="new_message_container" class="chat-composer">
        <%= render "messages/form", chat: @chat, message: @message %>
      </div>
    </div>

  </div>
</div>
