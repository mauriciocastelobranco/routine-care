<div class="rc-shell">
  <%= render "components/iphone_frame" do %>

    <div class="p-4">
      <h2 class="h5 fw-bold mb-1">Meus Pacientes</h2>
      <p class="text-muted small mb-3">
        Veja rapidamente quem está sob seu cuidado hoje.
      </p>

      <% if @patients.any? %>
        <div class="d-flex flex-column gap-3">
          <% @patients.each do |patient| %>
            <%= render "components/menu_card",
              title:       patient.name || "Paciente ##{patient.id}",
              description: (patient.respond_to?(:condition) ? patient.condition : "Sem condição cadastrada"),
              icon_emoji:  "🧑‍⚕️",
              path:        patient_path(patient) %>
          <% end %>
        </div>
      <% else %>
        <div class="text-center text-muted small mt-5">
          Nenhum paciente cadastrado ainda.
          <br>
          <span>Comece adicionando um novo paciente.</span>
        </div>
      <% end %>

      <div class="mt-4">
        <%= link_to "Adicionar Paciente",
          "#",
          class: "btn btn-success w-100 py-2 rounded-4 fw-semibold" %>
      </div>
    </div>

  <% end %>
</div>
