<div class="role-page" data-controller="role-selection">
  <!-- Banner (vídeo 2) -->
  <div class="home-banner">
    <video autoplay muted loop playsinline class="home-banner__video">
      <source src="<%= asset_path 'site_video_2.mp4' %>" type="video/mp4">
    </video>

    <div class="home-banner__overlay">

    </div>
  </div>
  <div class="role-shell">
    <div class="role-top">

      <h1 class="role-title">Como você vai usar o app?</h1>
      <p class="role-subtitle">Selecione o perfil que melhor descreve você</p>
    </div>

    <div class="role-list">
      <!-- FAMÍLIA -->
      <div class="role-item" data-action="click->role-selection#select"
           data-role-selection-target="item"
           data-role-value="family">
        <span class="role-left">
          <span class="role-icon role-icon--gray">👥</span>
        </span>

        <span class="role-center">
          <span class="role-name">Família (ADM)</span>
          <span class="role-desc">
            Responsável por acompanhar e gerenciar o cuidado
          </span>
        </span>

        <span class="role-dot" aria-hidden="true"></span>

        <%= link_to "", patients_path,
          class: "role-hidden-link",
          data: { role_selection_target: "link" } %>
      </div>

      <!-- CUIDADOR -->
      <div class="role-item" data-action="click->role-selection#select"
           data-role-selection-target="item"
           data-role-value="caregiver">
        <span class="role-left">
          <span class="role-icon role-icon--green">♡</span>
        </span>

        <span class="role-center">
          <span class="role-name">Cuidador</span>
          <span class="role-desc">
            Profissional ou familiar que cuida de alguém
          </span>
        </span>

        <span class="role-dot" aria-hidden="true"></span>

        <%= form_with url: caregivers_path,
              method: :post,
              class: "role-hidden-btn",
              html: { data: { role_selection_target: "form" } } do %>

          <button type="submit"
                  class="hidden-submit"
                  data-role-btn="caregiver">
          </button>
        <% end %>
      </div>
    </div>

    <button class="role-cta" type="button" disabled
            data-role-selection-target="cta"
            data-action="click->role-selection#confirm">
      Continuar
    </button>

    <p class="role-footnote">
    </p>
  </div>
</div>
