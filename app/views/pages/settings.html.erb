<div class="rc-shell">
  <%= render "components/iphone_frame" do %>
    <div class="p-4">

      <h2 class="h5 fw-bold mb-1">Configurações</h2>
      <p class="text-muted small mb-4">
        Ajuste seu perfil, notificações e preferências.
      </p>

      <div class="rc-card bg-white rounded-4 shadow-sm p-3 mb-3">
        <h6 class="fw-bold mb-2">Seu perfil</h6>
        <p class="mb-1"><strong>Nome:</strong> <%= current_user.first_name %> <%= current_user.last_name %></p>
        <p class="mb-1"><strong>E-mail:</strong> <%= current_user.email %></p>
        <% if current_user.role.present? %>
          <p class="mb-0"><strong>Perfil:</strong>
            <%= current_user.role == "caregiver" ? "Cuidador(a)" : "Paciente" %>
          </p>
        <% end %>
      </div>

      <div class="d-flex flex-column gap-2 mb-3">
        <%= render "components/menu_card",
          title:       "Editar perfil",
          description: "Nome, foto e contato",
          icon_emoji:  "👤",
          path:        edit_profile_path %>

        <%= render "components/menu_card",
          title:       "Notificações",
          description: "Lembretes e alertas",
          icon_emoji:  "🔔",
          path:        "#" %>

        <%= render "components/menu_card",
          title:       "Privacidade",
          description: "Dados e segurança",
          icon_emoji:  "🔒",
          path:        "#" %>
      </div>

      <%= link_to "Sair da conta",
        destroy_user_session_path,
        data: { turbo_method: :delete, turbo_confirm: "Deseja sair?" },
        class: "btn btn-outline-danger w-100 py-2 rounded-4 fw-semibold" %>

    </div>
  <% end %>
</div>
