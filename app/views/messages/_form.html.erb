<%= simple_form_for [chat, message] do |f| %>
  <%= f.input :content, label: "Em que posso ajudar?", required: true, placeholder: "Digite aqui a sua pergunta" %>
  <%= f.button :submit, 'Ask LLM', class: 'btn btn-primary' %>
<% end %>
